# 2026-06-26 — Release v0.9.1

**Scope:** Cut release 0.9.1 and fixed the failing `cargo audit` CI gate.

**Security fix (the reason CI was red):**

- `cargo audit` failed on **RUSTSEC-2026-0185** (HIGH, 7.5): remote memory
  exhaustion in `quinn-proto` from unbounded out-of-order QUIC stream reassembly.
- Fix: `cargo update -p quinn-proto` → `0.11.14` → `0.11.15` (lockfile-only,
  semver-compatible). Verified locally: `cargo audit` exit 0, `cargo check` OK.
- The remaining cargo-audit output is "unmaintained" _warnings_ (gtk-rs GTK3
  bindings, unic-\* crates) — they don't fail the gate.

**Release mechanics:**

- Bumped version 0.9.0 → 0.9.1 in `package.json`, `src-tauri/tauri.conf.json`,
  `src-tauri/Cargo.toml`; synced `Cargo.lock` via `cargo check`.
- CHANGELOG.md: new `## [0.9.1]` section (Added: OpenAI Compatible provider #12;
  Security: quinn-proto bump; Internal: CI audit hardening) + compare links.
- Committed as `chore(release): v0.9.1`, pushed master, **CI green** (all 5 jobs),
  then tagged `v0.9.1` → triggered `release.yml` (draft build, run 28226019555).

**Files affected:** `package.json`, `src-tauri/tauri.conf.json`,
`src-tauri/Cargo.toml`, `src-tauri/Cargo.lock`, `CHANGELOG.md`.

**Next steps:** When `release.yml` finishes, review the **draft** GitHub Release
(body = CHANGELOG [0.9.1]) and **Publish**. Verify `latest.json` lists all 3
platforms with inlined signatures (auto-update).
